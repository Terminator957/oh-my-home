# 家宴小本子 · 体验版上线前测试报告

日期:2026-07-26 · 被测项目:`jiayan-miniprogram`(AppID wx979824ce3247f715)

## 结论

**25 / 25 全部通过**,满足上传体验版的测试门槛。代码包 ~59 KB,远低于主包 2 MB 限制。

## 一、单元测试(7 项,node:test)

`unit.test.js` — 数据与纯逻辑:

| 用例 | 结果 |
|---|---|
| 菜品数据完整性:12 道菜,字段齐全,状态/分类/热量/星级合法 | ✔ |
| 菜名唯一 | ✔ |
| 心情映射的每道菜都存在于菜品库 | ✔ |
| starText 星级渲染(0/3/5 星) | ✔ |
| findDish 命中与未命中 | ✔ |
| 转盘候选池 = 想吃 + 常做(排除仅做过) | ✔ |
| 热量估算公式:60 + 食材数 × 54 | ✔ |

## 二、接口测试(5 项,node:test + wx storage mock)

`interface.test.js` — 数据层对页面暴露的存取契约(本版本无后端,数据接口即 storage 层):

| 用例 | 结果 |
|---|---|
| 今日菜单:默认空 → 添加 → 去重 → 覆盖 | ✔ |
| 客人购物车:默认预置 3 道,可增删清空 | ✔ |
| 购物车热量合计(1410 千卡)与人均(353)计算 | ✔ |
| 评分:默认取菜品星级,可覆盖持久化,互不影响 | ✔ |
| 评分边界:未知菜品默认 0 | ✔ |

## 三、UI 功能测试(13 项,miniprogram-automator 驱动真实模拟器)

`ui.test.js` — 通过开发者工具自动化端口在模拟器中真实点按:

| 用例 | 结果 |
|---|---|
| 首页加载,路径正确 | ✔ |
| 首页渲染出内容区 | ✔ |
| 首页渲染出「晚上吃点什么?」 | ✔ |
| 「换一个」切换今日推荐(冬瓜排骨汤 → 葱油面) | ✔ |
| 菜谱库「想吃」筛选生效(12 → 4 道) | ✔ |
| 详情页显示红烧肉与热量(520 千卡) | ✔ |
| 点星评分生效(rating=3) | ✔ |
| 客人加菜(3 → 4 道) | ✔ |
| 转盘开始旋转(spinning=true) | ✔ |
| 转盘停下并给出结果(椰子鸡) | ✔ |
| 滑卡「想吃」进清单并切下一张 | ✔ |
| 心情切到「想吃辣」,推荐列表更新 | ✔ |
| 定餐页合计热量 = 各菜之和 | ✔ |

## 运行方式

```bash
cd jiayan-tests
npm run test:unit       # 单元测试
npm run test:interface  # 接口测试
npm run test:ui         # UI 测试(需开发者工具开启服务端口,项目已用 cli auto 打开)
```

## 已知事项

- 上传体验版时服务端返回 `41002 appid missing`(见主报告),需确认 AppID 有效性与开发者权限后重试。
